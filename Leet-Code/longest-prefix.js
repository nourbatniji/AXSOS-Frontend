let arr = ["flower", "flight", "flow"]

function longestCommonPrefix(arr) {
   
    let firstEl = arr[0]


    for (let i = 0; i < firstEl.length; i++) {
        
        //while arr[i].indexof(firstel) !== 0 {
        //   firstel = firstel.slice(0, firstel.lenght-1)
        //      if firstel === "" return ""
        //. return firstel }

       let firstChara = firstEl[i]
        for (let j = 1; j < arr.length; j++) {
            //if firstEl[i] firstEl[i][j]  
            // ++
            //slice()
        }
    }
}


console.log(longestCommonPrefix(arr));
